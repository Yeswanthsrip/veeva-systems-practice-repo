Designing an OOP based program for collection of books,
where each book containd code,title,author,price.We must store the collection of unique books based on book code.Here code means(ISBN) number.
Now read any ISBN number of a book as a input & verify if it exists in the collection or not.
If exists display the details of the book.