You are given an array of n integers. Each integer in the array represents the time taken by a task to complete.

You are also given a time limit or threshold. Now find the maximum number of tasks that can be completed within the given time limit.

Example
n = 5, T = 6

a = [2, 3, 1, 5, 4]

output :- 3